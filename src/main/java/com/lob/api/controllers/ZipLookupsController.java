/*
 * LobLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.lob.api.controllers;

import com.lob.api.ApiHelper;
import com.lob.api.Server;
import com.lob.api.exceptions.ApiException;
import com.lob.api.exceptions.ErrorErrorException;
import com.lob.api.http.request.HttpMethod;
import com.lob.api.models.Zip;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class ZipLookupsController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public ZipLookupsController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Returns information about a ZIP code.
     * @param  zipCode  Required parameter: A 5-digit ZIP code.
     * @return    Returns the Zip response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public Zip zipLookup(
            final String zipCode) throws ApiException, IOException {
        return prepareZipLookupRequest(zipCode).execute();
    }

    /**
     * Returns information about a ZIP code.
     * @param  zipCode  Required parameter: A 5-digit ZIP code.
     * @return    Returns the Zip response from the API call
     */
    public CompletableFuture<Zip> zipLookupAsync(
            final String zipCode) {
        try { 
            return prepareZipLookupRequest(zipCode).executeAsync(); 
        } catch (Exception e) {  
            throw new CompletionException(e); 
        }
    }

    /**
     * Builds the ApiCall object for zipLookup.
     */
    private ApiCall<Zip, ApiException> prepareZipLookupRequest(
            final String zipCode) throws IOException {
        return new ApiCall.Builder<Zip, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/us_zip_lookups")
                        .formParam(param -> param.key("zip_code")
                                .value(zipCode))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .authenticationKey(BaseController.AUTHENTICATION_KEY)
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserialize(response, Zip.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Error",
                                (reason, context) -> new ErrorErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}